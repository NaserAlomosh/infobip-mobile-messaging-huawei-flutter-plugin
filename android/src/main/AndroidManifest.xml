<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Use only the SDK system tap broadcasts, not its duplicate local broadcasts. -->
        <receiver android:name="com.infobip.mobilemessaging.huawei.plugin.NotificationTapReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="org.infobip.mobile.messaging.NOTIFICATION_TAPPED" />
                <action android:name="org.infobip.mobile.messaging.interactive.NOTIFICATION_ACTION_TAPPED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
